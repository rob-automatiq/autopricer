-r requirements.txt

# Tests. httpx backs fastapi.testclient, so the API is exercised in-process
# without binding a port.
pytest==9.1.1
httpx==0.28.1

# Only for scripts/check_game_scope.py, the browser regression for the game
# selector. Not needed to run the app or the test suite.
playwright==1.63.0
